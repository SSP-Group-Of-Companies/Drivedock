/**
 * Admin Dashboard — Terminated (server entry)
 */
import TerminatedPageClient from "./TerminatedPageClient";

export const metadata = {
  title: "DriveDock Admin — Terminated",
};

export default function TerminatedPage() {
  return <TerminatedPageClient />;
}
